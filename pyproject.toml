[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-gym-fitness-assistant"
version = "1.0.0"
description = "AI Gym & Fitness Assistant"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["ai*"]